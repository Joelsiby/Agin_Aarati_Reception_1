import React from 'react';

export const InviteContent: React.FC = () => {
  // Clear overlay allowing full, unobstructed view of the background video
  return <div className="invite-scroll-area" />;
};
